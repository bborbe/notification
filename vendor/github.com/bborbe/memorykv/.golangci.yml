version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - revive
    - gosec
    - gocyclo
    - depguard
    - dupl
  settings:
    depguard:
      rules:
        Main:
          deny:
            - pkg: "github.com/pkg/erros"
              desc: "use github.com/bborbe/erros instead"
    errcheck:
      exclude-functions:
        - (io.Closer).Close
        - (io.Writer).Write
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln

    gosec:
      excludes:
        - G104
    govet:
      disable:
        - inline   # Go 1.27 govet inline flags stdlib generic calls (e.g. slices.BinarySearch) it cannot inline — noise, not a defect

  exclusions:
    presets:
      - comments
      - std-error-handling
      - common-false-positives
    rules:
      - linters:
          - staticcheck
        text: "SA1019"
      - linters:
          - revive
        path: "_test\\.go$"
        text: "dot-imports"
      - linters:
          - revive
        text: "unused-parameter"

formatters:
  enable:
    - gofmt
    - goimports
